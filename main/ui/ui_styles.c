/**
 * @file ui_styles.c
 * @brief Shared UI styles initialization
 */

#include "ui_styles.h"

void ui_styles_init(void)
{
    /* Currently colors are defined as macros.
     * Add lv_style_t objects here if shared styles are needed later
     * (e.g., common padding, border styles, animations). */
}
